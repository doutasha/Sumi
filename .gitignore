Dependencies
node_modules/

Build
dist/
*.local

# Tauri desktop (build outputs do Rust/bundler — nunca commitar)
desktop/src-tauri/target/
desktop/src-tauri/gen/
*.log

# Pacotes Windows prontos p/ distribuir (gerados via npm run package:windows)
build/